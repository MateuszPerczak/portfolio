import styled from "@emotion/styled";
import IconComponent from "./Icon.types";

const Icon: IconComponent = styled.span`
  font-family: "Segoe Fluent Icons", "Segoe Fluent Icons", "Segoe MDL2 Assets";
`;

export default Icon;
