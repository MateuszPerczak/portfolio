import styled from "@emotion/styled";
import { motion } from "framer-motion";

const StyledAnimationProvider = styled(motion.div)`
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  padding: 20px;
`;

export default StyledAnimationProvider;
