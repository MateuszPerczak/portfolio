import styled from "@emotion/styled";
import { motion } from "framer-motion";

const StyledPagesWrapper = styled(motion.div)`
  width: 100%;
  height: 100%;
  overflow: hidden;
`;

export default StyledPagesWrapper;
