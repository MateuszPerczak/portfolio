import styled from "@emotion/styled";
import { motion } from "framer-motion";

const StyledPageHeader = styled(motion.div)`
  font-size: clamp(0.8rem, 2vmax, 4rem);
`;

export default StyledPageHeader;
