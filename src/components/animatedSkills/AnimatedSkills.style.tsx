import styled from "@emotion/styled";
import { motion } from "framer-motion";

const StyledAnimatedSkills = styled(motion.div)`
  display: flex;
  flex-direction: row;
  gap: 10px;
  flex: 1;
  flex-wrap: wrap;
`;
export default StyledAnimatedSkills;
