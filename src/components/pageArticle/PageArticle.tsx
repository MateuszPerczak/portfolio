import styled from "@emotion/styled";

const PageArticle = styled.section`
  display: flex;
  max-width: 1000px;
  flex: 1 1 100%;
  padding: 0 1rem;
  width: calc(100% - 2rem);
  font-size: clamp(1rem, 5vw, 1.5rem);
`;

export default PageArticle;
