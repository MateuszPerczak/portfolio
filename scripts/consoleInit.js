const consoleInit = () => {
  console.log(
    "%c+",
    `font-size:1px; background:url('${window.location.origin}/img/consoleImage.png') no-repeat; padding: 300px 300px;`
  );
};

export default consoleInit;
